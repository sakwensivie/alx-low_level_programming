#include"main.h"
/**
 * rev_string - reverses the string
 * @s: input as character array
 * Return: none
 */
void rev_string(char *s)
{
	int i, j;
	char *c, tmp;

	c = s
	i = 0;

	while (1)
	{
		if (s[i] != '\0')
			i++;
		else
			break;
	}
	j = i;
	while (1)
	{
		if (j != 0)
		{
			c[j] = tmp;
			c
		}
		else
		{

}
