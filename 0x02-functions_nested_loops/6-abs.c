#include<main.h>
/*
 * _abs - Prints the absolute value of an integer
 *
 *
 * Return: returns the absolute value.
 */
int _abs(int x)
{
	if (x < 0)
	{
		x = -x;
		return (x);
	}
	else 
	{
		return (x);
	}
}
